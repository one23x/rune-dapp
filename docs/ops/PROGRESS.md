# Rune 项目进度看板

> **单一进度源**,Claude 维护。规则:
> - **详细记录**进下方「任务流水」(每步/每阶段都记)。
> - 每个里程碑完成时勾 `- [x]`,**Stop hook 自动把新完成项推到 Slack**(每阶段一条,不刷屏)。
> - Slack webhook 配在 `.claude/hooks/.slack-webhook`(当前为空占位,填入有效 URL 即生效)。

_最后更新:2026-06-08_

## 🔄 当前阶段
搭建协作基础设施(Slack 通知 / 配合规范 / 进度记录)。engine/workers 部署由你重开发中。

## ✅ 里程碑(勾选即推 Slack)
- [x] 2026-05-30 老服务器 sshd 根因定位+根治(acpid + suspend 拦截幽灵电源键)
- [x] 2026-05-30 env 凭证迁到美东(14 个文件 → `old-envs.tgz`)
- [x] 2026-05-30 数据库迁移 ai-engine-db → rune-prod-pg(2.4GB / 64 表,已验证一致)
- [x] 2026-05-30 训练数据 S3 跨区迁移(24.5MB → sagemaker-us-east-1)
- [x] 2026-05-30 代码 push:demo-rune(4028608)+ one-agents(48e7e27)→ one23x
- [x] 2026-05-30 SG 同步代码 + demo-rune hl.ts 3-way 合并(零冲突)
- [x] 2026-05-30 VPC peering SG↔US-east 确认 active(双向路由)
- [x] 2026-05-30 删除爱尔兰全栈(省 ~$190/月,留 RDS final snapshot)
- [ ] engine/workers 部署到美东(你重开发中)
- [x] 2026-05-30 协作基础设施搭建完成(Slack hook + 规范 + 文档)
- [x] 2026-06-08 会员交易数据可见性根治(视图绑定登录钱包 login_wallet)+ HL 余额同步消除引擎 API cap + admin 真实/调整后余额·持仓并排,dapp/admin 双端上线
- [x] 2026-06-08 Polymarket 真实持仓走 data-api 同步落 Supabase(替代不可靠 fills 推导)+ iOS 版本检测加 cache-bust

## 📋 任务流水(详细,持续追加 — 新的在最上)
- 2026-06-08 **Polymarket 真实持仓改走 data-api 同步,替代 fills 推导**:排查"PM 充值后看不到持仓"——根因链:①引擎源表 `trading.positions` 自 5-27 起空(新引擎 PM 用 deriveDepositWallet 不落表),sync 每 2min upsert 0 行;②试"从 fills 净额推导持仓"(`pm_rebuild_positions_from_fills` + 入 cron),但**实测 fills 不可信**:某账户一笔 Hormuz fill 记成对手 token(64982/Yes)+ 虚高份额($146),链上 ERC-1155 实为另一 token(65144/No)68.7 份额 $67.70;③份额既不在智能钱包也不在托管 EOA,而在 **Polymarket 代理钱包**(引擎派生)。**正解**:引擎 CF 代理 `…/trade/polymarket/users/:id/pusd-balance` 拿代理钱包+pUSD 余额 → Polymarket `data-api/positions?user=<代理>` 拿真实持仓 → 写 Supabase。脚本 `scripts/trading-sync/pm-positions-sync.mjs`(prod box pm2 单实例 `pm-positions-sync`,每 2min,全量替换该用户 PM 行=自动处理赎回;写 `trading_positions` + `trading_mark_prices` curPrice + 新表 `trading_pm_account_state` 代理/余额/市值)。现有视图/rollup/admin 读 trading_positions 自动生效,前端无需改。验证账户 0x2E85:Hormuz 68.7+US-Iran 29.98,**现金 $8.07+持仓 $91.84≈$100 完美对账**,幽灵 64982 消失。收尾:cron 移除 fills 推导(`pm_rebuild_*` 废弃)、清 37 行孤儿(含引擎 user_not_found)、新表 RLS public_read。**遗留**:那笔脏 fill 是引擎 PM 成交记录 bug(记成对手 token),值得引擎侧查;data-api 同步只覆盖引擎已建 PM 账户的用户。
- 2026-06-08 **dapp 三项 + iOS cache-bust 上线**:①持仓/统计每个金额旁加 %(MyPositionsTab 加净值格、PositionCard 加占净值%/ROE、DailyStatRow 各 pnl%,556270e);②iOS 版本检测 `safeReload` 改带 cache-bust 导航(`location.replace(url+?_v=ts)`,老 WKWebView 多一道保险,aa81f1a)。经 SG 中转 FF 推 GitHub + CF 部署(期间发现"交易数据"页 PM-only 是有意设计、撤回误改)。
- 2026-06-07 **首个自研 ML 模型 hl-copy-ranker:训练→部署→接入全跑上线(shadow)**(one-agents 61c47bf+84f14f9,worker 机生产运行)。意义:平台首个真正训练并上线服务的模型;此前训练管线长期饿死(signal_trades 决议=0)。改用 `trading.hl_copy_signals` leader 真实成交流的 FIFO round-trip realized PnL 作天然标签。①**数据**:`export_hl_training_set.py` 全表 mainnet→S3 `gold/hl_copy/v=20260607`,10340 样本/43.9% 正例/12 维因果特征(防泄漏)。②**训练**:撞账户级 SageMaker 训练配额=0(全新账户),云端 pipeline TrainRanker 失败→改 `train_hl_local.py` 本地 XGBoost,**test AUC=0.6572**(基线 0.5),logloss 0.6566<0.6910→证明 HL 跟单数据有可学习信号(Top 特征:leader 该币经验厚度/主流币/持仓名义/滚动胜率)。③**部署**:本地模型→SageMaker 格式→**serverless endpoint `hl-copy-ranker`**(InService,绕训练配额,按调用计费)。④**接入**:`ai/sagemaker.ts` hlCopyRankerScore()+`hyperliquid/hl-copy-ranker-features.ts`(线上复刻同套 FIFO 特征)+copy-executor 每条开仓信号算一次 P(盈利);env `SAGEMAKER_HL_RANKER_ENDPOINT=hl-copy-ranker`。**默认 shadow**(`HL_COPY_MIN_RANKER_SCORE`=0,只记 ai_inferences 不拦单),已验证分值落库 0 错误/延迟 80-120ms。⑤申请 ml.m5.2xlarge 训练配额(L-AD0A282D→2,PENDING)。踩坑:sagemaker SDK 默认装 v3(pin <3)、botocore 认 AWS_DEFAULT_REGION、bus 序列化后 happenedAt 是字符串须 coerce Date。**待**:shadow 攒数据核对训练分布后设 `HL_COPY_MIN_RANKER_SCORE=0.45` 开 gate;配额批后云端自动重训;容器内 AWS root key 轮换 scoped IAM。
- 2026-06-07 **首个自研 ML 模型 hl-copy-ranker:训练→部署→接入全跑上线(shadow)**(one-agents 61c47bf+84f14f9,worker 机生产运行)。意义:平台首个真正训练并上线服务的模型;此前训练管线长期饿死(signal_trades 决议=0)。改用 `trading.hl_copy_signals` leader 真实成交流的 FIFO round-trip realized PnL 作天然标签。①**数据**:`export_hl_training_set.py` 全表 mainnet→S3 `gold/hl_copy/v=20260607`,10340 样本/43.9% 正例/12 维因果特征(防泄漏)。②**训练**:撞账户级 SageMaker 训练配额=0(全新账户),云端 pipeline TrainRanker 失败→改 `train_hl_local.py` 本地 XGBoost,**test AUC=0.6572**(基线 0.5),logloss 0.6566<0.6910→证明 HL 跟单数据有可学习信号(Top 特征:leader 该币经验厚度/主流币/持仓名义/滚动胜率)。③**部署**:本地模型→SageMaker 格式→**serverless endpoint `hl-copy-ranker`**(InService,绕训练配额,按调用计费)。④**接入**:`ai/sagemaker.ts` hlCopyRankerScore()+`hyperliquid/hl-copy-ranker-features.ts`(线上复刻同套 FIFO 特征)+copy-executor 每条开仓信号算一次 P(盈利);env `SAGEMAKER_HL_RANKER_ENDPOINT=hl-copy-ranker`。**默认 shadow**(`HL_COPY_MIN_RANKER_SCORE`=0,只记 ai_inferences 不拦单),已验证分值落库 0 错误/延迟 80-120ms。⑤申请 ml.m5.2xlarge 训练配额(L-AD0A282D→2,PENDING)。踩坑:sagemaker SDK 默认装 v3(pin <3)、botocore 认 AWS_DEFAULT_REGION、bus 序列化后 happenedAt 是字符串须 coerce Date。**待**:shadow 攒数据核对训练分布后设 `HL_COPY_MIN_RANKER_SCORE=0.45` 开 gate;配额批后云端自动重训;容器内 AWS root key 轮换 scoped IAM。
- 2026-06-08 **会员交易数据可见性根治 + HL 余额同步 + admin 真实/调整对比上线**(4 个 rune-dev 子智能体并行):根因——面向用户视图按引擎派生 smart_wallet 归属,但会员连的是登录钱包(custodial 存 hl_master_address),247 账户大量 master≠smart,前端 `ilike(wallet)` 全落空;且 HL 余额从未进 Supabase,admin 实时打引擎撞 1万/天 cap(10517/10000)。①**DB 视图绑定连接钱包**(`scripts/trading-sync/bind-connected-wallet-views.sql`,幂等已 apply):5 个用户视图(open_positions/today_stats/today_closed/daily_history/v_trade_records)**末尾追加** `login_wallet`(=hl_master)+ `engine_eoa_address`(create or replace 不许中插改名,只能末尾追加);新建 `v_wallet_account_summary`(wallet/login_wallet/eoa/venue/account_value/position_value/unrealized/margin_used/available/margin_ratio,account_value=override 优先否则 NULL、margin=Σ名义/leverage、诚实不造现金);manual 持仓加 `leverage` 列、v_wallet_open_positions 暴露 leverage。验证 login_wallet=0x33D4 可查回 smart 0x608b BRENTOIL(leverage=10)。②**dapp 前端**(e98f1f7→部署 0f2b2904=rune-ai.io):trading-stats-hooks 五 hook 改 `.or(wallet/login_wallet/engine_eoa)` 匹配连接钱包,新增 useWalletAccountSummary;持仓行加杠杆徽章+每仓保证金,顶部保证金/可用走 account_summary、引擎兜底(`?? `保 0 有效)。③**HL 余额同步**(`hl-account-state-schema.sql` + `hl-account-state.mjs`,scp 到生产机 /home/ec2-user/trading-sync):新表 trading_hl_account_state(wallet/network/account_value/withdrawable/margin_used/total_ntl_pos/as_of,PK(wallet,network)),pm2 单实例 `hl-account-state` 每 2min 直接 POST HL 公共 clearinghouseState(地址 custodial=engine_eoa/agent=hl_master,免费不经引擎),12 行,样例 0x5222 净值$509.40 与 HL 实测一致;RLS 补 `using(true)` select policy + NOTIFY pgrst(否则 anon 读不到)。④**admin-panel**(7acb332→部署 3fd1f571 rune-admin-mainnet):HL 余额改读同步表(引擎 /v1/hl/account 调用归零),详情页真实(同步带 as_of)/调整后(override+「已覆盖」徽章)余额并排,持仓真实(纯 sync)/调整后(show=v_wallet_open_positions)切换+杠杆列+来源徽章;AddPosition/AddPmPosition 加杠杆输入。⑤**admin 交易记录全参数可编辑**(6fcbe9f 早些时候):行内编辑(manual 直改 / sync 隐藏原单+生成改后 manual 副本)+ 复制订单参数可改 + 批量复制 type/盈亏/金额覆盖。tsc 0 错、互盖均核对、Slack 已报(proj-rune-trading)。**待办**:PM 余额仍走引擎(量级低,可后续转同步表);trading_hl_account_state 的 RLS using(true) 暴露余额给 anon(沿用既有 trading 层口径,建议架构层确认);dapp 本地 commit e98f1f7 因本机 GitHub 403 未 push(经 SG 中转)。
- 2026-06-07 **统计架构转「订单驱动」+ admin/视图重构上线**:确立 real=纯真实(source=sync)/ show=真实+复制(sync+manual),admin 不再直接改数字、只能复制订单。①DB:order-driven-stats.sql(rollup v5 双轨,acct_daily 加 sync_* 列;show 持仓并入 manual add/replace 仓;breakeven wallet 修 smart)+ order-driven-views.sql(real_*→sync_*、展示值去 coalesce manual_*、is_overridden=show≠sync),已 apply 验证(0x69c0 PM show$66/real$0、HL show+4.75/real$0)。②admin(b1d285f→部署 63421692):删 EditDailyDialog/replace/hide/PM浮盈覆盖,新增 CopyOrdersDialog 跨账户复制(只 source=sync、盈亏筛选、勾选多笔×N、归属改写、写后 recalc),行内单笔复制保留。③清理:14 行 bkev 错 wallet 回填 smart、4 行残留 manual_* 清空、0x608b demo 三层叠加清除(0x69c0 演示保留)。④dapp 零影响(读 show 主列,列名未变;bkev 回填后连接钱包能查到 81 条历史单)。教训 L013(real 列名误导)。两份审计 agent 先行对账确认现状。
- 2026-06-07 **dapp overwrite 显示层上线 + admin 交易账户两版合并(L012 互盖复盘)**:dapp(dc16655→已部署 rune-lastest+push GitHub):①PM 余额 usePusdBalanceAdjusted 叠 trading_pm_overrides(两调用点);②/copy-trading/stats 收敛 PM-only(venueScope)+ 删「手动补录」标记(防穿帮);③PM/HL 历史记录统一 DailyStatRow(HL 专业风格胜出);④HL 手动历史过滤 record_hidden;⑤合并另一会话 b295bca(polygon-rpc.com 停用→publicnode),admin 同步换。admin(4954e94→已部署 rune-admin-mainnet+push):发现另一会话昨晚已重做交易账户页(0aa539d engine 代理余额/批量开户 + 3b1922a PM 手动持仓/隐藏记录)且被我中午的部署误盖 → 以其为基底移植本会话三能力:复制订单×N(联动盈亏)、写后 admin_trading_recalc() 即时刷统计、手动单补 user_id(基底版不带=不联动的 bug);恢复独立构建配置(脱 RUNE workspace)。教训入 LESSONS L012。
- 2026-06-07 **admin「交易账户」页重做为节点授权口径 + 全功能调控台,已部署 rune-admin-mainnet**:①入口从全量 trading_users 改为 v_admin_onboard_requests(节点+授权码 entitled,206 钱包)× 3 venue;②新视图 v_admin_trading_accounts(地址簿 PM smart/EOA + HL 主测网 follower、双轨统计 调整后+real_*、日/月/总 已实现&未实现 + 全 %:日月分母=持仓成本、总分母=授权码 cap)+ 新表 trading_pm_overrides + 补 pm_manual/record_hidden RLS + RPC admin_trading_recalc()(admin 写完即时刷统计)+ rollup v3(PM 手动单并入实现盈亏;**隐藏记录 = v_trade_records 与 rollup 双剔除**),全部在 scripts/trading-sync/admin-accounts-schema.sql(幂等,已 apply);③页面:真实/调整后一键切换、余额实查(HL 公共 info API 主测网分域 + PM Polygon 链上 pUSD 0xC011…6位小数,overrides 紫标)、调控台(加持仓 add/replace、平仓=hide真实/停用手动+平仓单联动、补订单、复制×N带时间抖动、隐藏/删除、当日统计 manual_* 覆盖);④联动冒烟:+$5 平仓单→日 0→5/总 26.25→31.25/% 同步,隐藏→回退,删除→恢复 ✓;⑤构建:SG rune-admin-panel(本地副本不可构建:Replit overrides 排除 darwin 二进制;SG pnpm-workspace.yaml 占位语法修为 onlyBuiltDependencies,补装 typescript tsc 0 错),wrangler 发 rune-admin-mainnet(部署前查 deployment list 防 L010)。⚠️ SG 仓库改动未 commit(3 新文件 + App/layout),待推 one23x/rune-admin-panel;旧版 630 行草稿备份 /tmp/trading-accounts.draft1.tsx.bak。
- 2026-06-07 **HL 跟单瘫痪六层根因修复 + HIP-3 builder DEX 支持上生产**(引擎 5 提交:e55cc70→286c85c→fd5f464→ec13b2f→343fd4c,已备份 GitHub `prod-live` 分支,⚠️生产机勿 pull main):①资金型 worker 锁(HL_COPY_EXECUTOR/LEADER_WATCH=false,treasury 遗留)解锁;②公共 API 429 → HL_API_RATE 4/s + HYPERLIQUID_INFO_URL 按类型分流(Alchemy 只支持 clearinghouseState/spot/meta,userFills/allMids 422 走官方);③NODE_GATING 等级查错钱包(托管 EOA 而非用户钱包→零额度→全部托管开仓静默跳过)→ 反查 users 表主人钱包取最大等级;④HIP-3 协议(perpDexs 索引、asset=100000+dexIdx*10000+i、meta/allMids/clearinghouseState 按 dex 查并合并,xyz dexIndex=1,公式被交易所真实下单验证);⑤builder DEX 独立保证金 → sendAsset(官方 SDK 核对的 user-signed action)开仓前自动划转(需求×1.1≥$12、≤cap×2、主账户留$1);⑥事故:HIP-3 补丁 am 冲突被管道吞码后带冲突标记构建上线 crash-loop ~2min,abort 回滚再经 SG worktree 正确合并(教训:**git am 不接管道;该 Dockerfile 不做类型检查,部署前必须独立 tsc**)。dapp 同日:策略包「已开启」leader+参数指纹双重判定(修选一包四卡全亮,288eb46)。诊断范例:用户 0x33D4(EOA)←hl_master 回查、托管 0x5222 充 200U forwarder 自动补 gas 转入 HL ✓。待:第一笔 xyz 自动划转+成交回执、0x36f8 充 POL、prod-live↔main 正式合并。
- 2026-06-07 **HL 充值三 Tab + gas 自动补给 + tx 详情页直链上线**(dapp 2cf98e6 已部署;引擎 gas-grant 已上生产):①HL 充值弹窗主网三 Tab(托管=直转/扫码/跨链→托管 EOA;agent=直转→HL Bridge2+引导页强警示不给二维码;直转自动切 Arbitrum,最低 5 USDC);②直转前查连接钱包原生币,不足自动调 POST /v1/rune/gas-grant(资格=授权码 OR 节点持有人 getNodeLevel>0,每钱包每链限 2 次,低于阈值才发,Arb 0.0003 ETH/Pol 0.1 POL,台账 trading.gas_grants);引擎部署:SG feat/gas-grant-clean(6b8378c,从混入 autoalign 的脏分支摘出)→format-patch→生产机 cherry 到 feb04ed 基线(e55cc70)→psql 建表→GAS_GRANT_ENABLED=true→docker build+up --no-deps backend→线上 smoke 403/400 全过。**待办:0x36f8 Polygon 0 POL,需充值 ~20 POL 否则 PM 侧发放失败**(Arb 有 0.005 ETH≈16 笔);③Supabase trading_trade_records/hl_manual_positions 加 tx_hash(已 apply),三视图尾部追加;前端行仅有 hash 才显示「查看链上记录」直达 explorer/tx/<hash>,地址列表页链接全删(防手动数据穿帮),手动/持平订单可复用真实 hash 多行同详情页(admin 面板录入 tx_hash 的 UI 在 rune-admin-panel,另会话仓库,待加)。遗留线上问题:pUSD 直转显示余额不足(疑似资金在 Polymarket proxy 钱包,等用户给测试地址)、跨链 PayEmbed "process failed"(等截图定位)。
- 2026-06-06 **大版本合并+上线**:feat-node-gating-pm-deposit(PM 充值三 Tab + stats 改造)合入 main;新增 ①钱包直转前主动 switchChain 到 Polygon 再拉起 pUSD transfer 授权(aed6a80) ②iOS 版本检测自动刷新(dfd2977:构建注入 __BUILD_ID__ + /version.json no-store + 切前台比对 reload + vite:preloadError 防白屏,根治 iOS Safari/WKWebView 旧 SPA 常驻内存拿不到新版;本次上线后老 iOS 用户仍需手动刷一次进入新机制);远端 5c507f6/4327d1f(pUSD 标签+deploy 脚本)经 SG bundle 拉回合并(fad789b,shared.tsx 冲突取本地三 Tab 新结构)。push 经 SG 中转(4327d1f→dfd2977);部署在 SG 执行 deploy:mainnet(坑:①嵌套 pnpm 被 corepack 切 11.5.0 与锁定 10.26.1 冲突 → 拆两步跑 ②wrangler token 变量名是 .env 的 CLOUDFLARE_API_TOKEN_1,需 export CLOUDFLARE_API_TOKEN="$CLOUDFLARE_API_TOKEN_1")。线上验证:version.json 200 + no-store。未合并遗留:safe-1u-014453(1 USDC 测试档)。
- 2026-06-06 admin-panel 新增「交易账户」页(/trading-accounts,已部署 rune-admin-mainnet):账户列表(连接钱包/托管EOA/HL网络/今日统计)→ 详情(每日统计真实vs修改双轨+行内编辑、当前持仓含手动行、全部交易记录、手动补订单/平仓/HL持仓)。配套 DB:trading_acct_daily 加 manual_* 覆盖列(rollup 只刷真实列,双轨并存,替代 is_manual 行锁),views 展示值=coalesce(manual,real)+附 real_* 列,v_wallet_open_positions 并入 admin 手动持仓,RLS admin_write(校验 admin_users)。admin 面板构建在 SG(~/projects/rune-admin-panel,需 PORT/BASE_PATH/VITE_SUPABASE_* env,outDir dist/public);本地副本已落后弃用。
- 2026-06-06 修复 /recruit 被覆盖事故:SG 侧从 d0c2833(无 recruit)二次部署盖掉本机版本 → 本机重部署恢复;随后统一代码:本地提交 9d60709,合并远端 PR #6 为 e893772(门控取远端 mode 版,PmTradeRecords 取远端,deposit-cap/HlDepositGuide 双保留),经 SG bundle 中转 push 回 GitHub main(本机 GitHub 账号 suspended 403,需解封/换 PAT),线上现为合并版。SG 另一会话的 unify/c44a60c-into-main 分支已无必要,待其放弃。
- 2026-06-06 HL 显示全面可手动调控 + 上线:新表 trading_hl_overrides(统计覆盖:净值/可提/浮盈/当日盈亏/跟单数,非null生效)+ trading_hl_manual_positions(持仓 add/replace/hide);前端 hl-display-overrides.ts useHlAccountAdjusted 包装 useHlAccount(3 个调用点全换),历史并入 trading_trade_records 手动行;新表被 Supabase 自动启 RLS 导致 anon 读空 → 加 public_read select policy(写仍拒);deploy:mainnet 已发(rune-lastest,wrangler 需 .env 的 CLOUDFLARE_ACCOUNT_ID/API_TOKEN 环境变量)。
- 2026-06-06 HL 标记价喂价上线:`scripts/trading-sync/hl-marks.mjs` 部署生产机 pm2(hl-marks,每 60s 拉 HL 主/测网 allMids → Supabase trading_mark_prices,venue=hl_mainnet/hl_testnet);rollup/持仓 view 改按网络取价;浮盈亏全真实化(测试网全账户浮亏 -$683,亏损监控抓到 4 个账户)。
- 2026-06-06 **修复 trading-sync 接错库**:生产引擎(one-agents-backend Docker)实际写 RDS `rune` 库,sync 一直读 `postgres` 库(旧引擎阶段数据,5/30 前 18 户)。已切源 `/postgres`→`/rune`(.env 备份 .env.bak-postgres-db;pm2 需 delete+start,restart --update-env 不生效因 RDS_URL 烤在进程 env);清理 Supabase 8 张镜像表旧行(trade_records 历史 1878 条 + is_manual 行保留);现同步 21 户/3026 HL 持仓。v_wallet_open_positions HL 改按币种聚合。前端新增 /copy-trading/stats(读新 views,替代 history 页)。
- 2026-06-06 Supabase 主网新增交易统计层(`scripts/trading-sync/stats-schema.sql`,additive 不动 sync):`trading_trade_records`(交易记录实体表,刷新只追加、手动补单永不被删改)、`trading_acct_daily`(每账户×venue×日快照,is_manual 保护)、`trading_mark_prices`(标记价,喂价后浮盈才非 0);views v_trade_records / v_wallet_daily_history / v_wallet_open_positions / v_wallet_today_closed / v_wallet_today_stats / v_loss_monitor_today;pg_cron trading-rollup-daily 每 10 分钟刷新。注意:HL 无 follower 成交流水(RDS 只有 leader 信号流 hl_copy_signals),HL 实现盈亏自 2026-06-06 起靠快照增量积累。
- 2026-05-30 搭建协作基础设施:.gitignore 堵 .claude 密钥、notify-slack.sh + Stop hook、PROGRESS/COLLABORATION/RUNBOOK/DECISIONS 文档。

## ⏳ 待办 / 风险
- **RDS final snapshot** `ai-engine-db-final-migrated`(eu-west-1)— 美东数据验证一段时间无误后可删,彻底省钱。
- **明文密钥 revoke** — `.claude/settings.local.json` 里有 3 个 GitHub PAT + Supabase 密码;迁移用的那个 PAT 也要 revoke。
- **demo-rune** 其余 41 个未提交文件 — 你正式 `git merge origin/master` 时处理(hl.ts 已合并,备份 `hl.ts.pre-merge.bak`)。
- **engine 不稳** — one-engine 在 SG 重启过 39 次,部署美东前查 `pm2 logs` 找根因。
